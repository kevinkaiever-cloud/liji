# 🚀 GitHub Pages 部署指南

本指南将帮助你轻松地将量子粒子模拟器部署到 GitHub Pages，让全世界都能访问你的项目！

## 📋 部署前提

1. **GitHub 账户** - 已注册并登录
2. **项目仓库** - 已推送到 `kevinkaiever-cloud/liji`
3. **Git 配置** - 本地已配置 Git 用户信息

## 🎯 部署方法

### 方法一：自动部署（推荐）

项目已配置自动化部署脚本，只需运行：

```bash
# 确保在项目根目录
cd particle-physics-website

# 安装部署依赖
npm install

# 执行部署命令
npm run deploy
```

这个命令会自动：
1. 构建生产版本
2. 推送到 `gh-pages` 分支
3. 触发 GitHub Pages 构建

### 方法二：手动部署

#### 步骤 1：启用 GitHub Pages
1. 访问仓库页面：https://github.com/kevinkaiever-cloud/liji
2. 点击 **Settings** 标签
3. 左侧菜单选择 **Pages**
4. 在 **Source** 部分：
   - 分支：选择 `master`
   - 文件夹：选择 `/ (root)`
5. 点击 **Save**

#### 步骤 2：等待构建完成
- GitHub 会自动开始构建
- 构建状态可在 **Actions** 标签查看
- 通常需要 1-3 分钟完成

#### 步骤 3：访问网站
构建完成后，访问：
```
https://kevinkaiever-cloud.github.io/liji/
```

## 🌐 自定义域名（可选）

如果你想使用自己的域名：

### 1. 配置 DNS
在域名服务商处添加 CNAME 记录：
```
类型: CNAME
名称: www（或 @ 表示根域名）
值: kevinkaiever-cloud.github.io
TTL: 自动
```

### 2. 在 GitHub 设置
1. 进入仓库 **Settings** → **Pages**
2. 在 **Custom domain** 输入你的域名
3. 点击 **Save**
4. 勾选 **Enforce HTTPS**（推荐）

### 3. 添加 CNAME 文件
在项目根目录创建 `CNAME` 文件：
```
yourdomain.com
www.yourdomain.com
```

## 🔧 故障排除

### 问题 1：页面显示 404
**可能原因**：
- GitHub Pages 尚未构建完成
- 构建失败
- 路径配置错误

**解决方案**：
1. 检查 **Actions** 标签中的构建状态
2. 确保 `public/index.html` 文件存在
3. 重新触发构建（修改并推送任意文件）

### 问题 2：样式/脚本加载失败
**可能原因**：
- 相对路径问题
- 缓存问题

**解决方案**：
1. 使用绝对路径：`/liji/styles.css`
2. 清除浏览器缓存
3. 检查控制台错误信息

### 问题 3：部署脚本失败
**可能原因**：
- 缺少 `gh-pages` 依赖
- Git 配置问题

**解决方案**：
```bash
# 安装依赖
npm install --save-dev gh-pages

# 检查 Git 远程仓库
git remote -v

# 重新部署
npm run deploy
```

## ⚡ 性能优化

### 1. 启用 HTTPS
GitHub Pages 默认支持 HTTPS，确保启用：
- Settings → Pages → Enforce HTTPS（勾选）

### 2. 缓存策略
GitHub Pages 自动设置缓存头：
- HTML: `max-age=600`（10分钟）
- CSS/JS: `max-age=31536000`（1年）

### 3. CDN 加速
GitHub Pages 使用全球 CDN，自动优化访问速度。

## 📊 监控与分析

### 1. 访问统计
GitHub 不提供访问统计，可集成：
- **Google Analytics** - 免费网站分析
- **Plausible** - 隐私友好的分析工具
- **Umami** - 开源分析平台

### 2. 性能监控
- **Google PageSpeed Insights**: https://pagespeed.web.dev/
- **WebPageTest**: https://www.webpagetest.org/
- **Lighthouse**（Chrome DevTools）

## 🔄 持续部署

### 自动构建
每次推送到 `master` 分支时，GitHub Actions 会自动：
1. 构建项目
2. 运行测试（如果配置）
3. 部署到 GitHub Pages

### 手动触发
如需手动触发部署：
1. 修改任意文件
2. 提交并推送
```bash
git add .
git commit -m "触发重新部署"
git push
```

## 🛡️ 安全建议

### 1. 仓库权限
- 保持仓库公开（Public）以便 GitHub Pages 访问
- 定期检查 Collaborators 权限

### 2. 依赖安全
```bash
# 检查安全漏洞
npm audit

# 自动修复
npm audit fix
```

### 3. 内容安全
- 避免在代码中存储敏感信息
- 使用环境变量管理配置

## 📱 移动端优化

项目已内置响应式设计，确保：
- ✅ 触摸交互支持
- ✅ 移动端布局适配
- ✅ 性能优化（60fps）

## 🌍 国际化支持

如需多语言支持：
1. 创建语言目录：`public/zh/`, `public/en/`
2. 配置重定向规则
3. 添加语言切换功能

## 🎨 主题自定义

### 修改主题颜色
编辑 `public/styles.css`：
```css
:root {
  --primary-color: #0ea5e9;
  --secondary-color: #8b5cf6;
  --background-color: #0f172a;
}
```

### 添加新主题
1. 在 CSS 中定义新主题类
2. 添加主题切换 JavaScript
3. 保存用户偏好到 localStorage

## 🔗 相关资源

### 官方文档
- [GitHub Pages 文档](https://docs.github.com/pages)
- [自定义域名指南](https://docs.github.com/pages/configuring-a-custom-domain-for-your-github-pages-site)
- [HTTPS 配置](https://docs.github.com/pages/securing-your-github-pages-site-with-https)

### 工具推荐
- [GitHub Actions](https://github.com/features/actions) - 自动化工作流
- [Jekyll](https://jekyllrb.com/) - 静态网站生成器（可选）
- [Netlify](https://www.netlify.com/) - 替代部署平台

## 📞 支持与帮助

### 遇到问题？
1. 查看 [GitHub Pages 故障排除](https://docs.github.com/pages/troubleshooting)
2. 在仓库 [Issues](https://github.com/kevinkaiever-cloud/liji/issues) 提问
3. 搜索 [GitHub Community](https://github.community/)

### 功能请求
欢迎提交 Issue 或 Pull Request！

---

**🚀 现在你的量子粒子模拟器已经准备好面向全世界了！**

访问 https://kevinkaiever-cloud.github.io/liji/ 分享给朋友吧！

*最后更新: 2026年2月24日*